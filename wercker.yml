box: wercker/default
build:
  steps:
    - script:
        name: koko
        code: sudo apt-get update
    - script:
        name: echo
        code: sudo apt-get install maven
    - script:
        name: echod
        code: mvn test
